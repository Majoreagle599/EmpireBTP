<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-md navbar-dark navbar-custom">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_accueil') }}">
                    🏗️ Empire BTP
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarCollapse">
                    <ul class="navbar-nav mx-auto mb-2 mb-md-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_reglement') }}">📜 Règlement</a>
                        </li>
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_chantiers') }}">🏗️ Mes Chantiers</a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="materielDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    🚜 Matériel
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="materielDropdown">
                                    <li><a class="dropdown-item" href="{{ path('app_materiel') }}">� Catalogue Matériel</a></li>
                                    <li><a class="dropdown-item" href="#">🏗️ Mon Parc Matériel</a></li>
                                    <li><a class="dropdown-item" href="#">📅 Locations en cours</a></li>
                                    <li><a class="dropdown-item" href="#">🔧 Maintenance & Réparations</a></li>
                                    <li><a class="dropdown-item" href="#">📊 Statistiques Matériel</a></li>
                                </ul>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_personnel') }}">👷 Personnel</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_marche') }}">🛒 Marché</a>
                            </li>
                        {% endif %}
                    </ul>
                    
                    <div class="d-flex flex-column align-items-end">
                        {# Première ligne: Mode sombre et Utilisateur #}
                        <div class="d-flex align-items-center mb-2">
                            {# Bouton mode sombre - toujours visible #}
                            <button id="darkModeToggle" class="btn btn-outline-light btn-sm me-2" title="Changer de thème">🌙</button>
                            
                            {% if app.user %}
                                <div class="dropdown me-2">
                                    <a class="btn btn-outline-light btn-sm dropdown-toggle" href="#" role="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                        👤 {{ app.user.email }}
                                    </a>
                                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                                        <li><a class="dropdown-item" href="#">👤 Mon Profil</a></li>
                                        <li><a class="dropdown-item" href="#">🎯 Objectifs/Missions</a></li>
                                        <li><a class="dropdown-item" href="#">📊 Statistiques</a></li>
                                        <li><a class="dropdown-item" href="#">🏆 Mes Succès/Badges</a></li>
                                        <li><a class="dropdown-item" href="#">📈 Tableau de bord</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" href="#">🔐 Changer mot de passe</a></li>
                                        <li><a class="dropdown-item" href="#">⚙️ Paramètres</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">🚪 Déconnexion</a></li>
                                    </ul>
                                </div>
                            {% else %}
                                <a class="btn btn-outline-light btn-sm me-2" href="{{ path('app_login') }}">🔑 Connexion</a>
                                <a class="btn btn-warning btn-sm" href="{{ path('app_register') }}">✨ Inscription</a>
                            {% endif %}
                        </div>
                        
                        {# Deuxième ligne: Dropdown Secteurs d'activité #}
                        <div class="dropdown">
                            <button class="btn btn-secteurs dropdown-toggle" type="button" id="secteursDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                                🏗️ Secteurs d'activité
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="secteursDropdown">
                                <li><a class="dropdown-item" href="{{ path('app_secteur_voirie') }}">🛣️ Voirie</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_secteur_carriere') }}">⛰️ Carrière</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_secteur_demolition') }}">💥 Démolition</a></li>
                                <li><a class="dropdown-item" href="{{ path('app_secteur_transport') }}">🚚 Transport</a></li>
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </nav>

        <main>
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }} alert-dismissible fade show container" role="alert">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            {% block body %}{% endblock %}
        </main>
    </body>
</html>
